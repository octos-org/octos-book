# Ch20 factcheck 报告(ch20-factcheck,C1,lane cheap)

- 基线:chapters/ch20-octoloop.md 与 book/src/part4/ch20.md 开工时自 master(e42e2f9)拷入,`cmp` 一致(174 行)。
- 源码基线实测:octoscode `git rev-parse --short=8` → `1129fa33` ✅;octos → `9c157101` ✅。
- 事实基准:assets/ch20-facts.md;R 类与 .rs 锚点逐条以 `git show 1129fa33:<file>` / `sed -n` 复跑,未采信 facts 表原文。

## 汇总(一句话)

代码侧锚点(olp_mcp.rs、outer_duty.rs、三个契约测试、octos CLI)与全部数字常量**逐条命中**;但 **协议文档 `OUTER_LOOP_PROTOCOL.md` 的 9 处行号锚点系统性 +6 偏移**(章节按 octoscode 工作树未提交状态写成,违反 1129fa33 只读基线),另有 3 处数字/语义错误。机械项与字数全部达标。

## 是否可定稿

**否——不可定稿。** 3 项 critical 须修(全部是行号/数字替换,不动论述);修完可交 master 复验。

---

## Critical(3 项,必须修)

### C1. `olp-board-append.sh` 三行核心行号错(章节 3 处引用)

章节声称 `exec 9>"$LOCK"`(:20)、`flock -x 9`(:21)、`cat >> "$BOARD"`(:22),合计引用「:20-22」3 处(20.3 节、20.6 节、回顾第 2 条)。

命令与输出(`git show 1129fa33:scripts/olp-board-append.sh | cat -n`):

```
20  trap 'rm -f "$TMP"' EXIT
21  cat > "$TMP"
22  exec 9>"$LOCK"
23  flock -x 9
24  cat "$TMP" >> "$BOARD"
```

实际三行是 **:22/:23/:24**。应全文替换「:20-22」→「:22-24」。

### C2. 协议文档行号系统性 +6(9 处),根因:按工作树未提交状态写稿

octoscode 工作树 `docs/OUTER_LOOP_PROTOCOL.md` 有未提交改动(`git status`:` M docs/OUTER_LOOP_PROTOCOL.md`,`git diff 1129fa33 --stat` → +6 行,在基线 :164 后插入 L3 平台扩展段)。章节所有 ≥190 的协议行号与工作树一致、与 1129fa33 基线不符:

| 章节引用 | 1129fa33 实际 | 命中内容 |
|---|---|---|
| v0 实验记录 :196(20.2 R1) | **:190** | `## v0 实验记录(2026-08-23…)` |
| 实战第二辑 :218(20.2 R2) | **:212** | `## 实战沉淀·第二辑…` |
| 多外环协作 :260 起(20.2) | **:253**(段首;:260 落段中) | `### 多外环并发…` |
| 裁决审计面 :322 起(20.2 R3) | **:271**(段首;:316 为段内「节奏」行) | `### 裁决审计:master 自决…` |
| 已知局限 :345(20.2 R5、20.3 ×2) | **:339** | `## 已知局限(v0)` |
| DefaultHasher 局限 :349(20.5) | **:343-345** | session-hash 条目 |
| 附录 A :354(20.2) | **:348** | `## 附录 A…` |
| 附录 B :375 / 双环矩阵 :393(20.2) | **:369 / :387** | `## 附录 B…` / `### 双环搭配矩阵` |

同源错位还包括:20.6 的 `OLP_OUTER_BOOT.md:34`(预算档)——实际「预算档(修订 5-10M / 切片 10-20M / 战役 30-50M)」在 **:44**(:34 是「## 1. 黑板」标题;facts 表 §1/§8 同错)。

修法二选一:①全部按上表改为基线行号(推荐,基线纪律);②若 octoscode 即将合并该 +6 diff,可保留行号但须在版本演化说明里声明「行号含 octoscode 工作树在途改动」,二者取一。

<details>以下锚点经基线复核**正确**:R1-R7 条款 :52/:67/:71/:74/:77/:89/:91/:103(`grep -n "R[0-9]b? —"` 全中)、版本头 :7、v1→v2 :12-15、角色 :19、信道矩阵 :27(:29/:39)、接入清单 :131-137(RFC3339/快照/落点三坑全中)、选型 :290(「:289 起」成立)、goal_03 案例 :279、AGENTS.md:3、GUIDE :221/:227/:229/:233、BOOT :60、olp_mcp.rs 全部(:21-32/:36/:46/:108/:125/:150/:174/:184-187/:189-196/:255/:275/:281/:286/:328,406 行)、outer_duty.rs(:34/:55/:63/:83/:99/:162/:220/:259/:311/:345,:345-380 PDEATHSIG 竞态 :357-371,476 行)、cmd 三件(:6/:8-10/:28/:45)、契约测试 25 用例 8+7+10 行号+函数名逐一命中(367/290/745 行)、octos 侧(steer.rs:1/:22、ledger.rs:1、peer.rs:54、goal.rs:340、goal_loop_runtime.rs:265/:298、master_continuation_scheduler.rs:147)、sqlite_ledger.rs:13(GoalLedger)与 6,360 行。</details>

### C3. 两处数字/语义错(octos 主仓)

1. 20.6 节「GoalLedger…39 个公开方法」:机械复核 `grep -c 'pub fn' crates/octos-fleet/src/sqlite_ledger.rs` → **43**(impl 块内 41)。39 与任何口径不符(facts 表同错)。改 43 或 41 并注明口径。
2. 20.6 节「`GoalRuntimeState`(:265)的五态里 budget_limited 是独立中间态」:@9c157101 实测 `GoalRuntimeState` 为**四态** `Active/Paused/Completed/Failed`(:265-271);`budget_limited` 不在该枚举,是 goal 状态字符串层的中间态(完成门 `active|budget_limited`,agent_orchestrator.rs:2208/2220)。行号 :265 对,「五态」错,应改为四态 + budget_limited 属 goal 状态机层。

## Major(2 项,建议修)

- **M1 活文件漂移**:20.3 节「黑板 465 行、29 个编号条目、71 处 ACK」是 facts 快照;主 checkout 黑板现为 **491 行 / 30 条目 / 85 ACK**(`grep -c` 实测,mtime 2026-09-03 04:50,整改期仍在追加)。建议加「截至 2026-09-03 快照」限定,或删具体计数。
- **M2 表述歧义**:20.2 R6「这是六条规则里唯一有机械验证兜底的版本纪律」——R1 ACK 语法同样有契约钉住(同节自引 :96)。建议改为「唯一被契约测试钉住的**版本一致性**纪律」。

## Minor(4 项,可不改)

- outer_duty.rs `as_str` 实为 **:44**(章节 :43;facts 同源)。
- GUIDE 动机句: :248 是角色表标题,「内环便宜、只花在判断上」文字在 :259(同节内,可接受)。
- 「Rust 源码 9 件」未列清单(30=7+2+9+3+5+4 自洽但 9 件不可机械复核),建议脚注列名。
- v0 「第 9 条 ACK wontdo」「signal-hook 走升级」两案例细节未能在基线机械定位(十条评审项 :193 ✅);建议 master 复验时顺带 grep。

## 机械项与字数(全部达标 ✅)

| 项 | 要求 | 实测(命令) | 判定 |
|---|---|---|---|
| mermaid | 4 | `grep -c '```mermaid'` → 4(围栏 8 全配对) | ✅ |
| 「——」 | ≤2 | `grep -o '——' \| wc -l` → 1 | ✅ |
| 加粗 | ≤15 | `grep -o '\*\*' \| wc -l` /2 → 11 | ✅ |
| 黑话 9 词 | 0 | 逐词 `grep -o`(赋能/抓手/闭环/颗粒度/沉淀/对齐/打通/击穿/拉通)→ 全 0 | ✅ |
| 字数(去代码汉字) | ≥5,000(master 实测 5,019) | `sed '/^```/,/^```/d'` 后 `grep -o '[一-鿿]'` → **5,019** | ✅ |
| 镜像 | cmp 一致 | `cmp chapters/ch20-octoloop.md book/src/part4/ch20.md` → 一致 | ✅ |
| SUMMARY | 有第 20 章条目 | `grep -n '第 20 章' book/src/SUMMARY.md` → :43 | ✅ |
| 自证命令 | 0 | 正文代码围栏仅 4 个 mermaid 块,bash 块 0 | ✅ |
| .rs 引用计数 | 27 | `grep -oE '[A-Za-z0-9_/.-]+\.rs(:[0-9]+(-[0-9]+)?)?'` → 27 | ✅ |

**占比 13.8% 无法核对**:章节正文无此数字;本章去代码汉字 5,019 / 全书含代码汉字 122,281 = 4.3%,/ part4(含 ch20)15,765 = 33%,没有任何实测口径得 13.8%。若 brief 要求正文声称占比,需 master 给出口径定义。

## 结论

- 机械面干净,代码锚点质量高;**定稿阻塞在 C1-C3 的行号/数字替换**(约 15 处机械修正,不动任何论述结构)。
- C2 须 master 裁决修法:按基线改行号,或声明在途 diff 口径——两种都成立,但不能保持现状(行号引用与自称基线 1129fa33 不符)。

---
*审查者:ch20-factcheck peer · 2026-09-03 · 只报告不改稿 · 本报告为唯一新增文件*
